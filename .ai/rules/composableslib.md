---
paths:
  - '{app/Actions/Series/**,app/Http/Controllers/Series/**,resources/js/{composables,lib}/**}'
---

# Composableslib

## Recover Show batches through exact reselection
Reload recovery never persists browser File handles or credentials. Require one exact verification set for every current pending/uploading/paused item, match normalized relative paths before unique suffixes/basenames, fingerprint bounded windows, and authorize only the first reconciled incomplete item.
