---
paths:
  - 'resources/js/**'
---

# Js

## Use Shows in public copy
Use “Shows” for every user-facing label, heading, breadcrumb, action, empty state, and accessibility name. Keep Series in internal models, routes, module paths, types, root kinds, and backend identifiers.
