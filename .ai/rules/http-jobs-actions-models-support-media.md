---
paths:
  - 'app/{Http,Jobs,Actions,Models,Support/Media}/**'
---

# Http Jobs Actions Models Support Media

## Manual scans are administrator-only and absence-safe
Recursive scans run only when manually requested by an administrator, never follow symlinks, and exclude .media-upload-manager. Treat every supported video as an independent finding. Only a healthy adopted disk may produce Missing; confirming external removal preserves media history while releasing the active path and current primary.
